---
name: 📦 Build and Publish

on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  setup-matrix:
    runs-on: ubuntu-latest
    outputs:
      build_matrix: ${{ steps.set-build-matrix.outputs.build_matrix }}
      openvox_release: ${{ steps.set-openvox-release.outputs.openvox_release }}
      openvox_version: ${{ steps.set-openvox-version.outputs.openvox_version }}
    steps:
      - name: Source checkout
        uses: actions/checkout@v6

      - name: 'Setup yq'
        uses: dcarbone/install-yq-action@v1.3.1

      - id: set-build-matrix
        run: echo "build_matrix=$(yq -o=json build_platforms.yaml | jq -c)" >> $GITHUB_OUTPUT

      - id: set-openvox-release
        run: echo "openvox_release=$(yq '.include[0].openvox_release' build_platforms.yaml)" >> $GITHUB_OUTPUT

      - id: set-openvox-version
        run: echo "openvox_version=$(yq '.include[0].openvox_version' build_platforms.yaml)" >> $GITHUB_OUTPUT

      - id: debug-output
        run: |
          echo $GITHUB_OUTPUT

  build-and-push-container:
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    needs: setup-matrix
    strategy:
      matrix: ${{ fromJson(needs.setup-matrix.outputs.build_matrix) }}
    steps:
      - name: Build Vox Pupuli Test Container v${{ matrix.openvox_release }}-${{ matrix.platform }}
        uses: voxpupuli/gha-build-and-publish-a-container@v2
        with:
          registry_password: ${{ secrets.GITHUB_TOKEN }}
          build_arch: linux/${{ matrix.platform }}
          buildfile: Containerfile
          docker_username: voxpupulibot
          docker_password: ${{ secrets.DOCKERHUB_BOT_ADMIN_TOKEN }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ matrix.openvox_release }}-${{ matrix.platform }}

  create-multiarch-manifests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    needs:
      - setup-matrix
      - build-and-push-container
    steps:
      - name: Log in to the ghcr.io registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to the docker.io registry
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: voxpupulibot
          password: ${{ secrets.DOCKERHUB_BOT_ADMIN_TOKEN }}

      - id: debug-output
        run: |
          echo "OpenVox Release: ${{ needs.setup-matrix.outputs.openvox_release }}"
          echo "OpenVox Version: ${{ needs.setup-matrix.outputs.openvox_version }}"

      - name: Create multiarch manifests
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_version }}-${{ github.ref_name }} \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_version }}-latest \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_release }}-${{ github.ref_name }} \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_release }} \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_release }}-latest \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/voxbox:latest \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t docker.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_version }}-${{ github.ref_name }} \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t docker.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_version }}-latest \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t docker.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_release }}-${{ github.ref_name }} \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t docker.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_release }} \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t docker.io/${{ github.repository_owner }}/voxbox:${{ needs.setup-matrix.outputs.openvox_release }}-latest \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

          docker buildx imagetools create -t docker.io/${{ github.repository_owner }}/voxbox:latest \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-arm64 \
            ghcr.io/${{ github.repository_owner }}/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64

  info-output:
    runs-on: ubuntu-latest
    name: Bundle Info
    needs:
      - setup-matrix
      - build-and-push-container
    container: ghcr.io/voxpupuli/voxbox:${{ github.sha }}-v${{ needs.setup-matrix.outputs.openvox_release }}-amd64
    steps:
      - name: bundle info
        run: |
          bundle check
          bundle env
          bundle outdated || true

  update-dockerhub-description:
    runs-on: ubuntu-latest
    needs:
      - create-multiarch-manifests
    steps:
      - name: Source checkout
        uses: actions/checkout@v6

      - name: Update Docker Hub Description
        uses: peter-evans/dockerhub-description@v5
        with:
          username: voxpupulibot
          password: ${{ secrets.DOCKERHUB_BOT_ADMIN_TOKEN }}
          repository: voxpupuli/voxbox
